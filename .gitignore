# Rust
/target
**/target

# SwiftPM
**/.build
**/.swiftpm

# Node / Bun
node_modules
dist
.vite

# VitePress (website)
website/.vitepress/cache
website/.vitepress/dist

# Reference repos (cloned for study, never vendored)
/references

# Electrobun build output
apps/desktop/build
apps/desktop/artifacts

# OS / editor
.DS_Store
*.log

# Agent scratch: throwaway worktrees and per-machine settings. launch.json is shared.
.claude/worktrees/
.claude/settings.local.json
.codex/run/
